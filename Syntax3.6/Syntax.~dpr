program Syntax;

uses
  Forms,
  Identifers in 'Identifers.pas',
  Map2 in 'Map2.pas',
  Unit_Syntax1 in 'Unit_Syntax1.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
